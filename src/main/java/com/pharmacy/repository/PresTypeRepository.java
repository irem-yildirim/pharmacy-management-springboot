package com.pharmacy.repository;

import com.pharmacy.entity.PresType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PresTypeRepository extends JpaRepository<PresType, Long> {
}
